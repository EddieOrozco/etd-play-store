

{% schema %}
{
  "name": "Utility Bar",
  "settings": [
    {
      "type": "checkbox",
      "id": "enable_utility_bar",
      "label": "Enable Utility Bar",
      "default": true
    },
    {
       "type": "select",
       "id": "utility_bar_width",
       "label": "Utility Bar Width",
       "options": [
            {
                "value": "full-width",
                "label": "Full Width"
            },
            {
                "value": "page-width",
                "label": "Page Width"
            }
        ],
        "default": "full-width"
    }
  ],
  "presets": [
    {
      "name": "Utility Bar",
      "category": "Header"
    }
  ]
}
{% endschema %}